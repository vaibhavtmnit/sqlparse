Once resolved code is prepared it must create summary of the code where it should explain the entire transformation that code is doing and basically every detail, it must create explanation of what is happening in that code as the names of the fields can make it too big do not include field names but table names are important i.e. describing how tables are created and the interact what are the nuances and caveats etc.
IF the resolved code is huge then chunk it and process it for easy understanding. use eisting chunker but with ovverlap and smalrt agent which knows that it will receive chunks of conigous codes to so it knowns.
