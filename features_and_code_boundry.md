# Feature seggregation

A table 'ABC' might be dependendent on table 'XYZ' and table 'MNO' for the fields such that the total feature set of table ABC are feature set either directly used or derived from table 'XYZ' and table 'MNO'. create an agent or deepagent which look at a relationship found returned by mining at the end of the loop and it check current chunk
